export const accuracy_score = (preds: number[], y: number[]): number => {
    return 0;
}

export const confusion_matrix = (preds: number[], y: number[]): number[][] => {
    return [];
}

export const crossval_predict = (x: number[][], y: number[], folds: number): number[] => {
    return [];
}